# Uplink · 使用说明

这一版的分工：**讯飞**负责"听"（实时语音识别，国内最成熟，长连接不掉线），**MiniMax** 负责"想 + 说"（对话和语音）。识别走你自己电脑上的后端，稳定可靠。

## 你需要准备

1. **Python**（多数电脑已自带；没有就去 python.org 装，勾选 "Add Python to PATH"）。
2. **MiniMax API Key**：platform.minimaxi.com 注册充值，「接口密钥」页创建。（负责对话+语音）
3. **讯飞 APPID + APIKey**：www.xfyun.cn 注册 → 创建应用 → 开通「实时语音转写」→ 在服务页复制 APPID 和 APIKey。（负责识别，有免费额度）

## 怎么启动

- Windows：双击 `启动Jake.bat`
- Mac：双击 `启动Jake.command`（首次若提示无法打开，右键→打开→确认）

首次会自动装组件（联网几十秒），之后浏览器自动打开 http://127.0.0.1:8800/。

## 首次使用

点「设置密钥」，把 MiniMax Key、讯飞 APPID/APIKey 都填上，选好声音，保存。
点「☕ 给 Jake 打电话」，允许麦克风（选"访问该网站时允许"）。

打电话后看页面「识别连接」那行：
- 显示 **已就绪（讯飞）** = 稳定识别生效，长通话不掉线 ✓
- 显示 **备用模式** = 讯飞没连上（检查 APPID/APIKey 是否填对、是否已开通「实时语音转写」服务），此时临时用浏览器识别顶着

## 换只耳朵：豆包（火山引擎）识别（可选）

除了讯飞，现在也支持**豆包（火山引擎）大模型流式识别**作为第二种「耳朵」，同样是后端长连接、稳定不掉线。二选一，填哪个用哪个：

1. 去 [火山引擎语音控制台](https://console.volcengine.com/speech/app) 创建应用 → 勾选「语音识别大模型 / 流式」→ 复制 **App ID** 和 **Access Token**（有免费额度）。
2. 在「设置密钥」里，把「识别引擎」选成 **豆包**，填入 App ID 和 Access Token，保存。
3. 打电话后「识别连接」那行显示 **已就绪（豆包）** 就是生效了。没连上会自动退回浏览器识别。

讯飞和豆包都留着、只选一个即可；哪家额度更合适、听得更准，就用哪家。

## 发音评测：让 Jake 真听到你的发音（强烈推荐）

开通后，Jake 让你跟读时会自动给你的**真实发音**打分（哪个词、哪个音发歪了），然后逐音纠正——这是发音教练闭环的核心。

1. 去 [讯飞控制台](https://console.xfyun.cn/) 打开你现有的应用 → 开通「**语音评测（流式版）**」（每日 500 次免费额度）。
2. 在应用页面复制 **WebAPI 的 APIKey 和 APISecret**（注意：和实时转写的 APIKey 不是同一个）。
3. 填到「通讯设置 → 讯飞框 → 发音评测」两个输入框里，保存。
4. 通话中 Jake 说 "your turn — try it once" 后你跟读，屏幕会出现 📊 评分条，Jake 会根据分数纠正你的发音。

不填这两个凭据也能正常通话，只是跟读环节没有真实打分。

## 识别热备份

豆包和讯飞凭据都填上时，通话中若一家掉线会**自动切到另一家**继续听（左下角显示「热备援」），不再退到浏览器识别。

## 为什么这样最稳

浏览器采麦克风 → 你电脑上的后端 → 后端用讯飞规范的长连接把音频流实时送去识别 → 文字回传。识别连接由后端维护、断线自动重连，比浏览器自带识别可靠得多。所有 Key 只存在你本机。

## 文件说明

- `启动Jake.bat` / `启动Jake.command` — 一键启动
- `server.py` — 后端（讯飞识别中转 + MiniMax 对话/合成 + 谈资 + 复盘）
- `static/index.html` — 网页界面
- `requirements.txt` — 依赖（启动脚本自动装）

